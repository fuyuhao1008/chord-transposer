[project]
requires = ["nodejs-24"]

[dev]
build = "pnpm install"
run = "pnpm dev --port 5000"

[deploy]
build = "pnpm install && pnpm build"
run = "cd .next/standalone/workspace/projects && PORT=5000 node server.js"

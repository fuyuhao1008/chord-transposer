# 环境变量示例（Vercel 部署时使用）
# 复制此文件为 .env.local 并填入实际值

# Node.js 环境（默认 production，无需手动设置）
# NODE_ENV=production

# 端口（Vercel 自动处理，无需手动设置）
# PORT=3000

# 如果使用数据库，请在此配置
# DATABASE_URL=

# 如果使用对象存储，请在此配置
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET_NAME=

# 环境变量示例（Vercel 部署时使用）
# 复制此文件为 .env.local 并填入实际值

# Node.js 环境（默认 production，无需手动设置）
# NODE_ENV=production

# 端口（Vercel 自动处理，无需手动设置）
# PORT=3000

# 如果使用数据库，请在此配置
# DATABASE_URL=

# 如果使用对象存储，请在此配置
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET_NAME=

# 视觉模型配置
# 可选值: coze | deepseek | openai
VISION_MODEL_TYPE=coze

# 视觉模型ID（指定具体模型）
# 如果不配置，将自动使用最佳纯视觉模型
# 可用的豆包视觉模型（按优先级自动选择）：
# - doubao-seed-1-6-vision-250815 (纯视觉模型，优先级最高)
# - doubao-seed-1-8-251228 (多模态模型)
# - doubao-seed-1-6-251015 (平衡性能模型)
#
# ⚠️ 注意：只有支持视觉的模型才能处理图片
# - doubao-seed-1-6-thinking-250715 (文本模型，不支持视觉)
# - doubao-seed-1-6-flash-250615 (文本模型，不支持视觉)
# - doubao-seed-1-6-lite-251015 (文本模型，不支持视觉)
#
# 注意：系统会自动选择可用的最佳模型
# - 优先使用纯视觉模型（识别更准确）
# - 纯视觉模型不可用时，自动切换到多模态模型
# - 支持多级回退机制
#
# 示例：
# VISION_MODEL=doubao-seed-1-6-vision-250815  # 指定使用纯视觉模型
# VISION_MODEL=  # 留空，让系统自动选择最佳模型

# API Key（优先使用此配置，如果没有配置则使用SDK默认）
COZE_API_KEY=your-coze-api-key-here

# 如果使用 DeepSeek 或 OpenAI，请在此配置
# VISION_API_KEY=your-api-key-here

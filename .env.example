# 环境变量示例（Vercel 部署时使用）
# 复制此文件为 .env.local 并填入实际值

# Node.js 环境（默认 production，无需手动设置）
# NODE_ENV=production

# 端口（Vercel 自动处理，无需手动设置）
# PORT=3000

# 如果使用数据库，请在此配置
# DATABASE_URL=

# 如果使用对象存储，请在此配置
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# AWS_BUCKET_NAME=

# 视觉模型配置
# 可选值: coze | deepseek | openai
VISION_MODEL_TYPE=coze

# 视觉模型ID（指定具体模型）
# 可用的豆包视觉模型：
# - doubao-seed-1-6-vision-250815 (专用视觉模型，当前默认)
# - doubao-seed-1-8-251228 (多模态Agent优化模型，支持视觉)
# - doubao-seed-1-6-251015 (平衡性能模型)
VISION_MODEL=doubao-seed-1-6-vision-250815

# 备用视觉模型ID（当主模型失败时自动切换）
FALLBACK_VISION_MODEL=doubao-seed-1-8-251228

# API Key（优先使用此配置，如果没有配置则使用SDK默认）
COZE_API_KEY=your-coze-api-key-here

# 如果使用 DeepSeek 或 OpenAI，请在此配置
# VISION_API_KEY=your-api-key-here
